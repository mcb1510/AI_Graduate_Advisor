# requirements.txt - Python Package Dependencies
# Install all these with: pip install -r requirements.txt

# Streamlit - Creates the web interface for the chatbot
# Version: Latest stable version
streamlit

# Requests - Makes HTTP requests to HuggingFace API
# This is how we talk to Mistral-7B
requests

# Python-dotenv - Loads environment variables from .env file
# This is how we securely load the API token
python-dotenv

# Sentence Transformers - For advanced RAG embeddings
# Version pinned to avoid breaking changes
sentence-transformers>=2.2.0,<4.0.0

# Scikit-learn - For machine learning utilities and metrics
# Version pinned to avoid breaking changes
scikit-learn>=1.3.0,<2.0.0

# NumPy - For numerical operations
# Compatible with recent versions
numpy>=1.24.0,<2.0.0

# streamlit
# pandas
# numpy
# faiss-cpu
# sentence-transformers
# transformers
# torch
# spacy